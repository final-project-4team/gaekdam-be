name: Create Release

on:
  pull_request:
    types: [closed]
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: create-release-develop-${{ github.event.pull_request.number || github.run_id }}
  cancel-in-progress: false

jobs:
  release:
    if: >-
      ${{
        github.event_name == 'workflow_dispatch' ||
        (github.event.pull_request.merged == true && github.event.pull_request.base.ref == 'develop')
      }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: develop

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: develop-release-${{ github.run_id }}
          name: develop-release-${{ github.run_id }}
          prerelease: true
          generate_release_notes: true
          target_commitish: develop
